{
  "name": "ai-solutions-website",
  "version": "1.0.0",
  "description": "Full-stack AI-Solutions website with React, Express, and PostgreSQL",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev:api": "dotenv -e .env -- pnpm --filter @workspace/api-server run dev",
    "dev:frontend": "dotenv -e .env -- pnpm --filter @workspace/ai-solutions run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./frontend\" --filter \"./backend\" --filter \"./database/**\" --if-present run typecheck",
    "db:push": "dotenv -e .env -- pnpm --filter @workspace/db run push",
    "db:seed": "dotenv -e .env -- pnpm --filter @workspace/scripts run seed",
    "codegen": "pnpm --filter @workspace/api-spec run codegen"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
